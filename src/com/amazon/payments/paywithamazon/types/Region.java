package com.amazon.payments.paywithamazon.types;

/*
* This enum class represents region codes for United States(US), United Kingdom(UK), 
* Germany(DE) and Japan(JP)
*/
public enum Region {
	DE, UK, US, JP;
}
